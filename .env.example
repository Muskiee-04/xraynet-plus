# Copy to .env and load with your tooling, or export in the shell.
# Never commit real keys.

GEMINI_API_KEY=
# Optional; default is gemini-2.0-flash
# GEMINI_MODEL=gemini-2.0-flash

# Existing X-ray model overrides (optional)
# XRAYNET_WEIGHTS=
# XRAYNET_ONNX_PATH=
